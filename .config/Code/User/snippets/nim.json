{
/*
	// Place your snippets for Nim here. Each snippet is defined under a snippet name and has a prefix, body and
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the
	// same ids are connected.
	// Example:
	"Print to console": {
		"prefix": "log",
		"body": [
			"console.log('$1');",
			"$2"
		],
		"description": "Log output to console"
	}
*/
	"alap": {
		"prefix": "alap",
		"body": [
			"import strutils    # strip, split, join",
			"import strformat   # &\"Hello {name}!\"",
			"import sequtils    # toSeq, map, filter",
			"import math        # sum",
			"",
			"import pykot       # input, toInt, toStr, toFloat",
			"",
			"proc main() =",
			"  echo \"hello\"$0",
			"",
			"#############################################################################",
			"",
			"main()",
		],
		"description": "basic Nim skeleton"
	},
	"sequence": {
		"prefix": "@",
		"body": [
			"@[$0]",
		],
		"description": "empty sequence"
	},
	"pure": {
		"prefix": "pure",
		"body": [
			"proc name$0(): ??? {.noSideEffect.} =",
		],
		"description": "pure function with no side effect"
	},
}
