options(browser="/usr/bin/firefox")
options(editor="/usr/bin/vim")
options(digits=5, help.try.all.packages=TRUE, help_type="html")

.First <- function() {
    cat("\nWelcome, ", Sys.getenv("LOGNAME"), "!\n", sep="")
}
